name: TODO_project_name         # replace with slug (hyphens → underscores), e.g. customer_orders
version: "1.0.0"
config-version: 2
profile: TODO_project_name      # must match the key in profiles.yml

model-paths:   ["models"]
test-paths:    ["tests"]
macro-paths:   ["macros"]
target-path:   "target"
clean-targets: ["target", "dbt_packages"]

models:
  TODO_project_name:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: ephemeral
    marts:
      +materialized: table
      +schema: marts
